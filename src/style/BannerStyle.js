import styled from "styled-components";

const Wrapper = styled.div`
  .carousel-wrapper {
    .carousel-root {
      .carousel {
        .slider-wrapper {
          .slider {
            .slide {
              img {
                height: 100vh;
                object-fit: contain;
              }
            }
          }
        }
      }
    }
  }
`;

export default Wrapper;
